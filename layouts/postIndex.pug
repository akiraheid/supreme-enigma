extends layout.pug

block content
	.row.justify-content-center: .col-auto: h1 Posts

	each post in collectionKeys.posts
		if post.path !== 'posts'
			.row.align-items-center
				small.col-2= post.date.toDateString()
				h2.col: a(href='/'+post.path) #{post.title}
