extends layout.pug

block content
	.row.justify-content-center: .col-auto: h1 Lists

	each list in indexData
		if list.path !== 'lists'
			.row.align-items-center
				h3.col: a(href='/'+list.path) #{list.title}
